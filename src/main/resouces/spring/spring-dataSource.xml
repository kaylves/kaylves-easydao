<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd">
	<!-- 加载配置文件 -->
	<bean id="config"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations">
			<list>
				<value>
					classpath:jdbc.properties
				</value>
			</list>
		</property>
	</bean>
	<!-- 配置连接数据配置s -->
	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close" depends-on="config">
		<property name="driverClassName">
			<value>${db.driver}</value>
		</property>
		<property name="url">
			<value>${db.url}</value>
		</property>
		<property name="username">
			<value>${db.user}</value>
		</property>
		<property name="password">
			<value>${db.pass}</value>
		</property>
		<property name="maxActive">
			<value>${db.max.active}</value>
		</property>
		<property name="maxIdle">
			<value>${db.max.idle}</value>
		</property>
		<property name="maxWait">
			<value>${db.max.wait}</value>
		</property>
		<property name="removeAbandoned">
			<value>${db.remove.abandoned}</value>
		</property>
		<property name="removeAbandonedTimeout">
			<value>${db.remove.abandoned.timeout}</value>
		</property>
		<property name="logAbandoned">
			<value>${db.log.abandoned}</value>
		</property>
		<property name="validationQuery">
			<value>${db.validation.query}</value>
		</property>
		<property name="testOnBorrow">
			<value>${db.test.on.borrow}</value>
		</property>
	</bean>
</beans>